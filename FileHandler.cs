using System;
using SplashKitSDK;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace splashkit
{
    public class FileHandler
    {
        public FileHandler()
        {
        }
        //string filePath = "/Users/andy/Documents/Uni/Semester_2/OOP/Week6/6.3D/splashkit/vehicles.txt";

        //List<string> vehicleLines = File.ReadAllLines(filePath).ToList();

        //foreach (string line in vehicleLines)
        //{
        //    int i;
        //    string[] entries = line.Split(',');
        //    int arrayIndex = int.Parse(entries[0]);
        //    double x = int.Parse(entries[2]);
        //    double y = int.Parse(entries[3]);

        //    Vehicle[] vehicle = new Vehicle[arrayIndex];

        //    for (i = 0; i < arrayIndex; i++)
        //    {
        //        vehicle[i] = new Vehicle(entries[1], x, y);
        //    }
        //    vehicle.

        //    _paths.Add(path);
        //}

        //using (StreamReader myFile = new StreamReader(filePath))
        //{
        //    int index = 0;
        //    while (!myFile.EndOfStream)
        //    {
        //        Employee E = new Employee();
        //        E.EmployeeNum = Int32.Parse(myFile.ReadLine());
        //        E.Name = myFile.ReadLine();
        //        E.Address = myFile.ReadLine();
        //        E.Wage = Double.Parse(myFile.ReadLine());
        //        E.Hours = Double.Parse(myFile.ReadLine());
        //        myEmployees[index++] = E;
        //    }
        //}
    }
}